Read-only independent design-delta review. Do not use tools, inspect other files, edit, delegate or implement. Goal: user cannot modify Buzz Hosted relay; user explicitly accepts a desktop-only sidebar showing flags only on already-loaded messages, not exhaustive history. Previous native extension is merged at8d644c70 but never deployed. No changes to hosted relay or user credentials permitted. Small reversible scope, existing UI patterns, no dependencies.
Proposed delta: replace ThreadFlags fetch/pagination/polling with passive observation of the existing React Query channelMessagesKey(channel.id), enabled:false so no new network request, no refetch buttons. Existing ChannelScreen owns the query and live subscription. Use canonical formatTimelineMessages(events,channel,currentPubkey,null) for reaction removal/deletion/edit and root semantics (TimelineMessage fields depth,parentId,rootId,kind,pending,reactions). Select committed kind9 depth0 roots without parent/root references; normalize FE0E/FE0F flags; completion wins; two flags one row. Display current edited root body using formatter semantics, searching only loaded bodies. Filter modes remain; loaded-only wording explicit, no all-history/count claims. Limit visible list to50 with local Show more, no fetch. Display error/pending state instead of cached rows when underlying query error, and clear on cache reset; key subtree by relay/account/channel. Only selected nonarchived stream channel with accessible membership OR open visibility mounts. Existing app community remount/reset owns clearing the shared query cache. No new persistent cache/provider or store. Original native command/server code remain unused by this panel for compatibility; document now dormant, not required.
Evidence from source: useChannelMessagesQuery(channel) owns getChannelWindowEvents,5min staleTime/1hgc, reconcileFetchedChannelWindow abort guards; live subscription reconciles changes into same key. formatTimelineMessages aggregates active reactions and applies deletion and authorized edit overlays; root fields available. Huddle transcript-specific resolvedMessages path exists and should not broaden this feature. Need verify observer enabled:false cannot introduce requests or rewrite source query behavior; tests should use real QueryClient cache updates.
Acceptance: no get_thread_flag_page invocation/no extra fetch or timer; roots only, completion+removal+deletion, text safe rendering, cache updates rerender, error/revocation/channel/account/relay switches do not retain rows; search clearly limited to loaded range. Full desktop suite, tsc/check and actual sidebar E2E through existing mocked live message/reaction events; native bundle build. Original server whole-history acceptance explicitly superseded by user. Report severity, exact design location, evidence or uncertainty, smallest correction. Do not claim unavailable code inspected. Favor existing authority rather than new parallel status logic.
