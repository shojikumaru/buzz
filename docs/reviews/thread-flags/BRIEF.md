# Native thread flags: design delta review
Read-only independent design review. Do not edit, run tools, commit, or delegate. Assess only this brief. Return severity, location, evidence, correction and verdict. Identify uncertainties. Goal: GitHub shojikumaru/buzz-thread-flags issue3, native Buzz sidebar for selected channel. Existing dependency-free prototype is reviewed; focus only new boundaries.
Acceptance: root kind9 messages only; any active reactor's 🚩 coordinating, 🔴 progressing, ☑️ completed (variation selectors normalized). Completed overrides active visibility; dual flags one row. Completed searchable. Removal recomputes state. No autoarchive or real process-status claim. Authorized channel only. Stable DESC timestamp / ASC ID cursor; same-second pages no skip. Stale responses after account/community/channel switch cannot appear. Deleted roots excluded. Historical query not limited to currently loaded messages.
Pinned host base813bbd14121edacc6cb4733301a3af12131aa10e. Existing POST/query does NIP98, tenant membership, cached channel authorization plus repair before query extensions; it emits relay-signed synthetic overlays 39005 summaries/39006 bounds. Tauri query_relay returns typed Nostr events. New proposal:
1. New focused DB store/thread_flags.rs method uses tenant community + channel binds, events(kind9,deleted_at null), LEFT JOIN canonical thread_metadata depth null/0 (not broadcast replies), active reactions removed_at null with community/event timestamp/id joins; canonical distinct flags. No new tables/index/dependency/daemon. Filter modes active/coordinate/progress/complete/all; title substring q bounded200 chars. Explicit limit50,max100; limit+1 probe. Capture cursor on returned ordered final row. Query time live state (not cross-page snapshot). Current content search, output limited title preview.
2. Strict `thread_flags` POST/query extension handled after existing auth and before normal fallback. Exactly one filter/one #h, kinds[9], cursor time+64hex both or neither, reject incompatible fields/modes. Inaccessible channel errors; same tenant authority as existing queries. Return exactly one new relay-signed query-only overlay kind39007 (confirm unused; register relay-only) containing protocol version/channel/filter identity, rows {id,pubkey,created_at,title,flags}, has_more,next_cursor. Old relay missing overlay => explicit unsupported, never silently empty.
3. Focused Tauri command uses query_relay (existing signer/auth), expected relay and signer fencing before/after await; validates signed overlay, version/channel/filter/cursor/row shape, bounds. Trust pinned current relay public key as for existing overlays; investigate key lookup before implementation. No client-cached membership grants. No new HTTP endpoint.
4. Small native React feature with focused API wrapper; one mount in AppSidebar only for selected channel. Active list default, coordinate/progress/complete/all filter and debounced search, load more; existing onOpenSearchResult navigation. Query key includes community relay/account/channel/mode/q. Hide old data on errors and switch; de-duplicate pages. Shared existing channel subscription invalidates reaction/delete/message changes, bounded refresh fallback and manual refresh while mounted only. No singleton cache, per-thread socket, or persisted state DB. Adapter implements documented prototype policy with provenance; no new npm dependencies. Snapshot demo unchanged.
5. Tests: DB postgres actual add/remove/completion/dual/root-vs-reply/deleted/tenant/cursor/q; bridge strict inputs + denied channels; Tauri malformed overlay/scope; React real list interaction, stale switch/error/navigation/event refresh. Run required repository just ci / touched whole-package suites and real UI+relay workflow where possible, report infrastructure blockers accurately. Owner merge/release separate. GitHub fork only, original checkout untouched. Does design miss a correctness/security boundary or add avoidable complexity? Propose smallest correction, not speculative platform redesign.
