## lens: custom: Independent read-only architecture review. Do not implement or delegate. Follow the neutral brief and report evidence, severity, correction and verdict.
- **[MEDIUM] design-delta §3, clause "Trust pinned current relay public key …; investigate key lookup before implementation"** — Evidence: the signing-key lookup/rotation semantics for the new 39007 trust anchor are explicitly deferred and undefined in the delta. Failure: relay rotates or serves a different signing key for 39007; the command either rejects every response (feature dead) or a lenient fallback accepts an unpinned signer and renders a fabricated flag list as authoritative. Correction: resolve and document the key-lookup/rotation rule before implementation and fail closed on any signer mismatch; no heuristic fallback.

- **[MEDIUM] design-delta §2, clause "kind39007 (confirm unused; register relay-only)"** — Evidence: the kind allocation is unconfirmed at review time; "confirm unused" is an open action, not a verified fact. Failure: 39007 is already emitted by the relay for another schema; a signature-valid but semantically foreign payload partially passes row-shape validation and corrupts the sidebar list. Correction: complete the registry confirmation and pin a verified-unused kind as a merge gate before any implementation.

- **[MEDIUM] design-delta §1, clause "LEFT JOIN canonical thread_metadata depth null/0"** — Evidence: NULL depth conflates "is a root" with "metadata row missing or lagged". Failure: a reply ingested before its `thread_metadata` row lands is classified as a root and appears in the root-only flags list, violating acceptance "root kind9 messages only". Correction: define root as `depth = 0` (or explicit root tag) and state documented behavior for metadata-less kind9 events (exclude, or include only under a backfill guarantee).

- **[LOW] design-delta §1–§2, clauses "title substring q bounded200 chars" / "Explicit limit50,max100" vs §2 reject list** — Evidence: §2's rejection list (filter/#h/kinds/cursor pairing, "incompatible fields/modes") does not state where q-length, limit-range, case-folding, or Unicode normalization are enforced, nor reject-vs-truncate. Failure: client sends q of 5,000 chars or limit=5000; server silently truncates/clamps, so returned rows match a different query than the cached key and re-navigation shows inconsistent pages. Correction: extension must 422 on q>200 and limit outside 1..100; specify ILIKE/casefold and NFC normalization identically in store and overlay validation.

- **[LOW] design-delta §5, test inventory** — Evidence: acceptance requires variation-selector normalization and the "old relay missing overlay ⇒ explicit unsupported" path; §5 lists dual/completion/cursor/q, strict bridge, malformed overlay/scope, React flows — no normalization-equivalence test and no missing-overlay error test. Failure: a relay-side ☑/☑️ normalization change or overlay-absent regression ships undetected, silently violating two stated acceptance criteria. Correction: add a DB normalization-equivalence test and a Tauri/bridge test asserting the explicit unsupported error when the overlay is absent.

- **[LOW/UNCERTAINTY] design-delta §4, clause "Shared existing channel subscription invalidates reaction/delete/message changes"** — Evidence: design assumes all flag reactions and their removals/deletions arrive on the channel subscription feed; coverage of the reaction event set is not enumerated. Failure: a reaction or its deletion carried on a different tag/community route never triggers invalidation and the list shows stale flags until the bounded fallback fires. Correction: enumerate the reaction/delete kinds the subscription covers; if partial, state the fallback refresh bound explicitly.

- **[LOW/UNCERTAINTY] design-delta §1, clauses "No new tables/index" + unanchored substring `q`** — Evidence: substring search over all tenant-channel kind9 events with a stated no-new-index constraint and no stated existing-index assumption. Failure: a large channel makes each debounced keystroke query run a sequential scan at perceptible latency. Correction: verify the existing (channel, kind, created_at) index bounds the scan in `ci`; only reopen the index constraint with measurements, not speculatively.

Uncertainties: kind-9 semantics and the thread_metadata writer/backfill ordering are taken from context, not verifiable in this brief; relay-side overlay ingestion (whether `query_relay` persists synthetic events) is assumed to match the existing 39005/39006 pattern and is unconfirmed here.

VERDICT: ISSUES
