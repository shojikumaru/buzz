# Review dispositions (in progress)
Base: block/buzz 813bbd14121edacc6cb4733301a3af12131aa10e. GitHub fork shojikumaru/buzz. Source checkout untouched. Implementer Astra/alpha-codex.
Kimi K3: reviewed isolated brief, requested high reasoning in design scope (CLI exposes no effort flag; actual effort unverified). GLM5.2:16000 thinking,32000 max tokens. Claude Opus5:high pending.
- Kimi1/2: accepted. Canonical SQL flags strip all FE0E/FE0F at query only, never mutate reaction history. active=(coordinate OR progress) AND NOT complete; coordinate/progress also exclude complete; complete requires complete; all any flag. Payload retains all flags.
- Kimi3: literal case-insensitive content substring using strpos(lower(content),lower(bound q)), no wildcard language or NFC normalization beyond Postgres lower; q max200 Unicode scalar values, limit1..100 strict rejection.
- Kimi4/5: cursor is last emitted row, probe excluded. Reaction joins community+root timestamp+root ID; no inherited reply flags.
- GLM1: existing identity_archive::fetch_relay_self_at provides NIP11 self, strict key shape, URL-scoped TTL cache; signed response must match exact captured relay key and verify(). No lenient rotation fallback; refresh after cache expiry, explicit error in meantime. query_relay_at_with_keys pins URL+signer snapshot throughout auth/read; before/after active scope assertions.
- GLM2: rg39007 across crates,desktop/src,desktop/src-tauri/src returned no matches at pinned base. Adjacent 39005/39006 are query-only signed overlays. Register relay-only39007, d identifies protocol request; never store/subscribe to overlay.
- GLM3: include only depth0 canonical metadata (ingest stores metadata atomically with event). Legacy metadata-less messages excluded conservatively; no whole-history backfill in this task. Document this limitation. Root-depth tests enforce.
- GLM4/5: strict shared Rust request types/validation, missing overlay explicit unsupported, variation-selector equivalence add/remove tests.
- GLM6: simplify first release to manual Refresh plus visible-panel30s refresh, immediate clear on refresh/errors/scope change. No extra WebSocket. Existing app events may add invalidation later; explicitly not instantaneous process status.
- GLM7: existing idx_events_community_channel_created(community_id,channel_id,created_at DESC,id) and idx_reactions_event(community_id,event_id,event_created_at). Measure query on fixture. No speculative index.
- Additional owner review: fresh DB access check per request (cached gate alone not sufficient for revocation), tenant+channel+viewer authorization included in same SQL snapshot as rows. Reject archives for active panel (no historical autoarchive); fail closed/empty on loss of access. On any polling error hide previous pages. Limit rendered pages to5 and reset to head on refresh; no accumulating background history fetch.
- Native policy must have one authoritative filter implementation: SQL derives flags and mode predicates; UI only displays server classification. Rust shared contract/flag constants document semantics; prototype remains separately pinned reference, no client reclassification.
## Opus5 high: conditional GO triage
B1 accepted: INNER reaction candidate join, GROUP BY roots, mode HAVING/outer WHERE and q BEFORE limit+1. Mixed-direction predicate explicit. No Rust filtering after limit.
B2/B3 accepted as above, depth0 only; all modes pinned.
B4 partially accepted: use captured configured relay URL (existing app authority) and its NIP11 self, verify signature and exact signer; d includes entire canonical request. Out-of-band key pinning is NOT an existing host facility; adding it would change host trust policy, so not adopted. NIP11 over configured relay's TLS has same authority as existing identity_archive agent membership path. This proves overlay originated from configured relay, not independent trustworthiness of that relay. No key from returned event is accepted as trust anchor. Failure rejects, no fallback. This is reviewer assumption corrected from inspected host implementation.
B5 accepted: dispatch after repair and fresh accessible-channel check; DB row query repeats viewer scope in same statement. Existing middleware rate gate remains outside handler.
N1-N9: literal body substring, preview240 Unicode chars whitespace-collapsed with "Untitled thread" UI fallback; stable flag order; future versions fail; refresh resets head only; root-only helper text; current active reaction rows authoritative regardless reactor later leaving (same host semantics); future timestamps retain existing sort behavior. No new keys, modes SQL mapping only.
Design frozen for implementation after all three reviews. No high finding left untriaged. Host implementation review required before PR ready.
